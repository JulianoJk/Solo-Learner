# Replace placeholders with actual values

CONNECTION_STRING="server=localhost;port=<port_number>;user=<database_username>;password=<database_password>;database=<database_name>"

# Root password for MySQL

MYSQL_ROOT_PASSWORD=root
