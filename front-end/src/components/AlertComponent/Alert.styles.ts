import { createStyles } from "@mantine/core";
import { COMMON_BLACK } from "../../Theme/Theme";
export const useStyles = createStyles((props) => ({
  alertBox: {
    margin: 10,
    border: `1px solid ${COMMON_BLACK}`,
  },
}));
